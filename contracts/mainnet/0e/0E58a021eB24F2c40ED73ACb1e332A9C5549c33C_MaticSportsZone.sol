¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦          ¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦       ¦¦¦     ¦¦¦     ¦¦¦¦¦¦¦¦¦¦¦¦¦     ¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦      ¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦¦
      ¯        ¯¯  ¯                 ¯            ¯¯¯¯¯¯¯¯¯¯¯  ¯            ¯¯¯¯¯¯¯¯¯¯¯  ¯         ¯       ¯       ¯¯¯¯¯¯¯¯¯¯¯       ¯¯¯¯¯¯¯¯¯¯¯  ¯¯¯¯¯¯¯¯¯¯¯  ¯        ¯¯  ¯¯¯¯¯¯¯¯¯¯¯ 
                                                                                                                                                                                        

 * 8% ROI DAILY
 * 2920% APR
 * 2% Developer Fee
 * 12% Referral Fee

*/

// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;


abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor () {
      address msgSender = _msgSender();
      _owner = msgSender;
      emit OwnershipTransferred(address(0), msgSender);
    }


    function owner() public view returns (address) {
      return _owner;
    }

    
    modifier onlyOwner() {
      require(_owner == _msgSender(), "Ownable: caller is not the owner");
      _;
    }

    function renounceOwnership() public onlyOwner {
      emit OwnershipTransferred(_owner, address(0));
      _owner = address(0);
    }

    function transferOwnership(address newOwner) public onlyOwner {
      _transferOwnership(newOwner);
    }

    function _transferOwnership(address newOwner) internal {
      require(newOwner != address(0), "Ownable: new owner is the zero address");
      emit OwnershipTransferred(_owner, newOwner);
      _owner = newOwner;
    }
}

contract MaticSportsZone is Context, Ownable {

    using SafeMath for uint256;

    uint256 private NFT_TO_ENTER_1MINERS = 1080000;
    uint256 private PSN = 10000;
    uint256 private PSNH = 5000;
    uint256 private devFeeVal = 2;
    bool private initialized = false;
    address payable public developer = payable(0x80066e3cAA43dFc3bA41c2AF94395C88dc5fb889);
    mapping (address => uint256) private NFTMiners;
    mapping (address => uint256) private claimedNFT;
    mapping (address => uint256) private lastHatch;
    mapping (address => address) private referrals;
    uint256 private marketNft;
    
    constructor() {
        _transferOwnership(developer);
    }
    
    function reinvest(address ref) public {
        require(initialized);
        
        if(ref == msg.sender) {
            ref = address(0);
        }
        
        if(referrals[msg.sender] == address(0) && referrals[msg.sender] != msg.sender) {
            referrals[msg.sender] = ref;
        }
        
        uint256 NftUsed = getMyNft(msg.sender);
        uint256 newMiners = SafeMath.div(NftUsed,NFT_TO_ENTER_1MINERS);
        NFTMiners[msg.sender] = SafeMath.add(NFTMiners[msg.sender],newMiners);
        claimedNFT[msg.sender] = 0;
        lastHatch[msg.sender] = block.timestamp;
        
        //send referral
        claimedNFT[referrals[msg.sender]] = SafeMath.add(claimedNFT[referrals[msg.sender]],SafeMath.div(NftUsed,8));
        
        //boost market to nerf miners hoarding
        marketNft = SafeMath.add(marketNft,SafeMath.div(NftUsed,5));
    }
    
    function sell() public {
        require(initialized);
        uint256 hasNft = getMyNft(msg.sender);
        uint256 NftValue = calculateNftSell(hasNft);
        uint256 fee = devFee(NftValue);
        claimedNFT[msg.sender] = 0;
        lastHatch[msg.sender] = block.timestamp;
        marketNft = SafeMath.add(marketNft,hasNft);
        payable(developer).transfer(fee);
        payable(msg.sender).transfer(SafeMath.sub(NftValue,fee));
    }
    
    function nftRewards(address adr) public view returns(uint256) {
        uint256 hasNft = getMyNft(adr);
        uint256 NftValue = calculateNftSell(hasNft);
        return NftValue;
    }
    
    function buy(address ref) public payable {
        require(initialized);
        uint256 nftBought = calculateNftBuy(msg.value,SafeMath.sub(address(this).balance,msg.value));
        nftBought = SafeMath.sub(nftBought,devFee(nftBought));
        uint256 fee = devFee(msg.value);
        payable(developer).transfer(fee);
        claimedNFT[msg.sender] = SafeMath.add(claimedNFT[msg.sender],nftBought);
        reinvest(ref);
    }
    
    function calculateTrade(uint256 rt,uint256 rs, uint256 bs) private view returns(uint256) {
        return SafeMath.div(SafeMath.mul(PSN,bs),SafeMath.add(PSNH,SafeMath.div(SafeMath.add(SafeMath.mul(PSN,rs),SafeMath.mul(PSNH,rt)),rt)));
    }
    
    function calculateNftSell(uint256 nft) public view returns(uint256) {
        return calculateTrade(nft,marketNft,address(this).balance);
    }
    
    function calculateNftBuy(uint256 bnb,uint256 contractBalance) public view returns(uint256) {
        return calculateTrade(bnb,contractBalance,marketNft);
    }
    
    function calculateNftBuySimple(uint256 bnb) public view returns(uint256) {
        return calculateNftBuy(bnb,address(this).balance);
    }
    
    function devFee(uint256 amount) private view returns(uint256) {
        return SafeMath.div(SafeMath.mul(amount,devFeeVal),100);
    }
    
    function seedMarket() public payable onlyOwner {
        require(marketNft == 0);
        initialized = true;
        marketNft = 108000000000;
    }
    
    function getBalance() public view returns(uint256) {
        return address(this).balance;
    }
    
    function getMyMiners(address adr) public view returns(uint256) {
        return NFTMiners[adr];
    }
    
    function getMyNft(address adr) public view returns(uint256) {
        return SafeMath.add(claimedNFT[adr],getNftSinceLastHatch(adr));
    }
    
    function getNftSinceLastHatch(address adr) public view returns(uint256) {
        uint256 secondsPassed=min(NFT_TO_ENTER_1MINERS,SafeMath.sub(block.timestamp,lastHatch[adr]));
        return SafeMath.mul(secondsPassed,NFTMiners[adr]);
    }
    
    function min(uint256 a, uint256 b) private pure returns (uint256) {
        return a < b ? a : b;
    }
}

library SafeMath {

    function tryAdd(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            uint256 c = a + b;
            if (c < a) return (false, 0);
            return (true, c);
        }
    }

    function trySub(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b > a) return (false, 0);
            return (true, a - b);
        }
    }

    function tryMul(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (a == 0) return (true, 0);
            uint256 c = a * b;
            if (c / a != b) return (false, 0);
            return (true, c);
        }
    }

    function tryDiv(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b == 0) return (false, 0);
            return (true, a / b);
        }
    }

    function tryMod(uint256 a, uint256 b) internal pure returns (bool, uint256) {
        unchecked {
            if (b == 0) return (false, 0);
            return (true, a % b);
        }
    }

    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        return a + b;
    }

    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return a - b;
    }

    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        return a * b;
    }

    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return a / b;
    }

    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return a % b;
    }

    function sub(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b <= a, errorMessage);
            return a - b;
        }
    }

    function div(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b > 0, errorMessage);
            return a / b;
        }
    }

    function mod(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        unchecked {
            require(b > 0, errorMessage);
            return a % b;
        }
    }
}